# Web evidence refreshed for v0.4

Checked 2026-09-13:
- Nav2 Jazzy custom planner tutorial documents `nav2_core::GlobalPlanner`, lifecycle hooks and `createPlan(start, goal, cancel_checker)`.
- `ros_gz` compatibility table lists ROS 2 Jazzy ↔ Gazebo Harmonic binaries at packages.ros.org.
- VRX ROS2/Gazebo Sim is the recommended starting point for new users; the project provides a Jazzy branch.
- GeographicLib `LocalCartesian` defines latitude/longitude in degrees and local Cartesian coordinates in meters, using WGS84 geocentric conversion by default.

URLs are intentionally documented for reproducibility; current final-answer citations provide the live verification.
